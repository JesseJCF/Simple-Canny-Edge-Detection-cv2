# Simple-Canny-Edge-Detection-cv2

EdgeDetect.py = barebones edge detection

ThresholdingDemo.py = app with sliders for upper and lower thresholds, plus sigmaX (which controls Gaussian blur, a preprocessing step)

OpenCV documentation: https://docs.opencv.org/3.4/da/d22/tutorial_py_canny.html
